import test from"node:test";import assert from"node:assert/strict";import{createFocusSession,evaluateBlocking,scheduleActive,validateDomain}from"../packages/blocking-engine/rules.js";
test("validates domains",()=>{assert.equal(validateDomain("WWW.Instagram.com"),"instagram.com");assert.throws(()=>validateDomain("bad"))});
test("focus blocks and override wins",()=>{const focus={active:true,endsAt:2000,blockedDomains:["instagram.com"],level:"hard"};assert.equal(evaluateBlocking({domain:"instagram.com",now:1000,focus,limits:[],overrides:{}}).blocked,true);assert.equal(evaluateBlocking({domain:"instagram.com",now:1000,focus,limits:[],overrides:{"instagram.com":1500}}).blocked,false)});
test("daily limit starts at budget",()=>{const limits=[{enabled:true,domain:"x.com",minutes:30,level:"soft"}];assert.equal(evaluateBlocking({domain:"x.com",now:1000,limits,usageSeconds:1799,overrides:{}}).blocked,false);assert.equal(evaluateBlocking({domain:"x.com",now:1000,limits,usageSeconds:1800,overrides:{}}).blocked,true)});
test("overnight schedules",()=>{const s={enabled:true,days:[1],start:"22:00",end:"06:00"};assert.equal(scheduleActive(s,new Date("2026-08-17T23:00:00")),true);assert.equal(scheduleActive(s,new Date("2026-08-18T12:00:00")),false)});
test("focus validation",()=>{const f=createFocusSession({durationMinutes:45,blockedDomains:["x.com","x.com"]},1000);assert.equal(f.endsAt,2701000);assert.deepEqual(f.blockedDomains,["x.com"]);assert.throws(()=>createFocusSession({durationMinutes:-1,blockedDomains:["x.com"]}))});
