import test from "node:test";import assert from "node:assert/strict";import{buildSummary,focusScore}from"../packages/analytics/summary.js";
const item=(seconds,level,category="Work",domain="work.test",startedAt=0,tabId=1)=>({activeSeconds:seconds,classification:{productivityLevel:level,category},domain,startedAt,tabId});
test("aggregates productivity, categories, domains and switches",()=>{const s=buildSummary([item(60,"productive","Work","a.test",0,1),item(30,"distracting","Social","b.test",60000,2),item(10,"neutral","Other","a.test",90000,1)],{from:0,to:100000});assert.deepEqual(s.totals,{active:100,productive:60,neutral:10,distracted:30});assert.deepEqual(s.byDomain,[{name:"a.test",seconds:70},{name:"b.test",seconds:30}]);assert.equal(s.tabSwitches,2);assert.equal(s.productiveInterruptions,1)});
test("focus score is bounded and fully explained",()=>{const result=focusScore([item(2700,"highly_productive")]);assert.equal(result.score,100);assert.equal(result.factors.reduce((s,x)=>s+x.maximum,0),100);assert.equal(focusScore([]).score,0)});
test("distracting time lowers focus score",()=>{assert.ok(focusScore([item(600,"productive")]).score>focusScore([item(600,"distracting")]).score)});
